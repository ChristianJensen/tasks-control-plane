name: Dispatch Agents
on:
  push:
    branches: [main]
    paths: ['queue/**']
  workflow_dispatch: {}

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Scan queue and dispatch agents
        env:
          GH_TOKEN: ${{ secrets.DISPATCH_PAT }}
        run: python3 scripts/dispatch-agents.py --cp-dir "$GITHUB_WORKSPACE"
