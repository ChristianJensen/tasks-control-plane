name: Validate Contracts
on:
  push:
    branches: [main]
    paths: ['contracts/**']
  pull_request:
    paths: ['contracts/**']
jobs:
  lint:
    uses: ChristianJensen/Relay/.github/workflows/validate-contracts.yml@main
    secrets: inherit
